import * as react from 'react';
import { ComponentPropsWithoutRef } from 'react';
import { Primitive } from '@radix-ui/react-primitive';
import * as rainbow_sprinkles from 'rainbow-sprinkles';

declare const foreground: {
    primary: `var(--${string})`;
    'primary-darker': `var(--${string})`;
    secondary: `var(--${string})`;
    'secondary-darker': `var(--${string})`;
    success: `var(--${string})`;
    'success-darker': `var(--${string})`;
    warning: `var(--${string})`;
    'warning-darker': `var(--${string})`;
    danger: `var(--${string})`;
    'danger-darker': `var(--${string})`;
    hint: `var(--${string})`;
    'hint-darker': `var(--${string})`;
    contrast: `var(--${string})`;
    'contrast-darker': `var(--${string})`;
    accent: `var(--${string})`;
    normal: `var(--${string})`;
    'normal-lighter': `var(--${string})`;
};
declare const sprinkles: rainbow_sprinkles.SprinklesFn<[{
    config: {
        position: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "position";
            vars: {
                default: string;
            };
        };
        display: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "display";
            vars: {
                default: string;
            };
        };
        alignItems: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "alignItems";
            vars: {
                default: string;
            };
        };
        justifyContent: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "justifyContent";
            vars: {
                default: string;
            };
        };
        flexDirection: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "flexDirection";
            vars: {
                default: string;
            };
        };
        gap: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "gap";
            vars: {
                default: string;
            };
        };
        alignContent: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "alignContent";
            vars: {
                default: string;
            };
        };
        padding: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "padding";
            vars: {
                default: string;
            };
        };
        paddingTop: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "paddingTop";
            vars: {
                default: string;
            };
        };
        paddingBottom: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "paddingBottom";
            vars: {
                default: string;
            };
        };
        paddingLeft: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "paddingLeft";
            vars: {
                default: string;
            };
        };
        paddingRight: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "paddingRight";
            vars: {
                default: string;
            };
        };
        margin: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                [-500]: string;
                [-200]: string;
                [-600]: string;
                [-800]: string;
                "-000": string;
                "-050": string;
                [-700]: string;
                [-900]: string;
                [-300]: string;
                [-100]: string;
                [-400]: string;
                "-025": string;
                "-075": string;
                [-150]: string;
                [-175]: string;
                [-225]: string;
                [-250]: string;
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "margin";
            vars: {
                default: string;
            };
        };
        marginTop: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                [-500]: string;
                [-200]: string;
                [-600]: string;
                [-800]: string;
                "-000": string;
                "-050": string;
                [-700]: string;
                [-900]: string;
                [-300]: string;
                [-100]: string;
                [-400]: string;
                "-025": string;
                "-075": string;
                [-150]: string;
                [-175]: string;
                [-225]: string;
                [-250]: string;
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "marginTop";
            vars: {
                default: string;
            };
        };
        marginBottom: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                [-500]: string;
                [-200]: string;
                [-600]: string;
                [-800]: string;
                "-000": string;
                "-050": string;
                [-700]: string;
                [-900]: string;
                [-300]: string;
                [-100]: string;
                [-400]: string;
                "-025": string;
                "-075": string;
                [-150]: string;
                [-175]: string;
                [-225]: string;
                [-250]: string;
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "marginBottom";
            vars: {
                default: string;
            };
        };
        marginLeft: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                [-500]: string;
                [-200]: string;
                [-600]: string;
                [-800]: string;
                "-000": string;
                "-050": string;
                [-700]: string;
                [-900]: string;
                [-300]: string;
                [-100]: string;
                [-400]: string;
                "-025": string;
                "-075": string;
                [-150]: string;
                [-175]: string;
                [-225]: string;
                [-250]: string;
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "marginLeft";
            vars: {
                default: string;
            };
        };
        marginRight: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                [-500]: string;
                [-200]: string;
                [-600]: string;
                [-800]: string;
                "-000": string;
                "-050": string;
                [-700]: string;
                [-900]: string;
                [-300]: string;
                [-100]: string;
                [-400]: string;
                "-025": string;
                "-075": string;
                [-150]: string;
                [-175]: string;
                [-225]: string;
                [-250]: string;
                '000': `var(--${string})`;
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "marginRight";
            vars: {
                default: string;
            };
        };
        width: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
            };
            name: "width";
            vars: {
                default: string;
            };
        };
        height: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
            };
            name: "height";
            vars: {
                default: string;
            };
        };
        minWidth: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
            };
            name: "minWidth";
            vars: {
                default: string;
            };
        };
        minHeight: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
            };
            name: "minHeight";
            vars: {
                default: string;
            };
        };
        maxWidth: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
            };
            name: "maxWidth";
            vars: {
                default: string;
            };
        };
        maxHeight: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '025': `var(--${string})`;
                '050': `var(--${string})`;
                '075': `var(--${string})`;
                '100': `var(--${string})`;
                '150': `var(--${string})`;
                '175': `var(--${string})`;
                '200': `var(--${string})`;
                '225': `var(--${string})`;
                '250': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
            };
            name: "maxHeight";
            vars: {
                default: string;
            };
        };
        border: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "border";
            vars: {
                default: string;
            };
        };
        borderRadius: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                '000': `var(--${string})`;
                '050': `var(--${string})`;
                '100': `var(--${string})`;
                '200': `var(--${string})`;
                '300': `var(--${string})`;
                '400': `var(--${string})`;
                '500': `var(--${string})`;
                '600': `var(--${string})`;
                '700': `var(--${string})`;
                '800': `var(--${string})`;
                '900': `var(--${string})`;
            };
            name: "borderRadius";
            vars: {
                default: string;
            };
        };
        backgroundColor: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                'rgb-primary': `var(--${string})`;
                primary: `var(--${string})`;
                'rgb-secondary': `var(--${string})`;
                secondary: `var(--${string})`;
                'rgb-success': `var(--${string})`;
                success: `var(--${string})`;
                'rgb-warning': `var(--${string})`;
                warning: `var(--${string})`;
                'rgb-danger': `var(--${string})`;
                danger: `var(--${string})`;
                'rgb-hint': `var(--${string})`;
                hint: `var(--${string})`;
                'rgb-contrast': `var(--${string})`;
                contrast: `var(--${string})`;
                'rgb-normal': `var(--${string})`;
                normal: `var(--${string})`;
                'normal-lighter': `var(--${string})`;
                'normal-darker': `var(--${string})`;
                'blue-050': `var(--${string})`;
                'blue-100': `var(--${string})`;
                'blue-200': `var(--${string})`;
                'blue-300': `var(--${string})`;
                'blue-400': `var(--${string})`;
                'blue-500': `var(--${string})`;
                'blue-600': `var(--${string})`;
                'blue-700': `var(--${string})`;
                'blue-800': `var(--${string})`;
                'blue-900': `var(--${string})`;
                'cyan-050': `var(--${string})`;
                'cyan-100': `var(--${string})`;
                'cyan-200': `var(--${string})`;
                'cyan-300': `var(--${string})`;
                'cyan-400': `var(--${string})`;
                'cyan-500': `var(--${string})`;
                'cyan-600': `var(--${string})`;
                'cyan-700': `var(--${string})`;
                'cyan-800': `var(--${string})`;
                'cyan-900': `var(--${string})`;
                'grape-050': `var(--${string})`;
                'grape-100': `var(--${string})`;
                'grape-200': `var(--${string})`;
                'grape-300': `var(--${string})`;
                'grape-400': `var(--${string})`;
                'grape-500': `var(--${string})`;
                'grape-600': `var(--${string})`;
                'grape-700': `var(--${string})`;
                'grape-800': `var(--${string})`;
                'grape-900': `var(--${string})`;
                'gray-000': `var(--${string})`;
                'gray-050': `var(--${string})`;
                'gray-100': `var(--${string})`;
                'gray-200': `var(--${string})`;
                'gray-300': `var(--${string})`;
                'gray-400': `var(--${string})`;
                'gray-500': `var(--${string})`;
                'gray-600': `var(--${string})`;
                'gray-700': `var(--${string})`;
                'gray-800': `var(--${string})`;
                'gray-900': `var(--${string})`;
                'gray-950': `var(--${string})`;
                'green-050': `var(--${string})`;
                'green-100': `var(--${string})`;
                'green-200': `var(--${string})`;
                'green-300': `var(--${string})`;
                'green-400': `var(--${string})`;
                'green-500': `var(--${string})`;
                'green-600': `var(--${string})`;
                'green-700': `var(--${string})`;
                'green-800': `var(--${string})`;
                'green-900': `var(--${string})`;
                'lime-050': `var(--${string})`;
                'lime-100': `var(--${string})`;
                'lime-200': `var(--${string})`;
                'lime-300': `var(--${string})`;
                'lime-400': `var(--${string})`;
                'lime-500': `var(--${string})`;
                'lime-600': `var(--${string})`;
                'lime-700': `var(--${string})`;
                'lime-800': `var(--${string})`;
                'lime-900': `var(--${string})`;
                'orange-050': `var(--${string})`;
                'orange-100': `var(--${string})`;
                'orange-200': `var(--${string})`;
                'orange-300': `var(--${string})`;
                'orange-400': `var(--${string})`;
                'orange-500': `var(--${string})`;
                'orange-600': `var(--${string})`;
                'orange-700': `var(--${string})`;
                'orange-800': `var(--${string})`;
                'orange-900': `var(--${string})`;
                'pink-050': `var(--${string})`;
                'pink-100': `var(--${string})`;
                'pink-200': `var(--${string})`;
                'pink-300': `var(--${string})`;
                'pink-400': `var(--${string})`;
                'pink-500': `var(--${string})`;
                'pink-600': `var(--${string})`;
                'pink-700': `var(--${string})`;
                'pink-800': `var(--${string})`;
                'pink-900': `var(--${string})`;
                'red-050': `var(--${string})`;
                'red-100': `var(--${string})`;
                'red-200': `var(--${string})`;
                'red-300': `var(--${string})`;
                'red-400': `var(--${string})`;
                'red-500': `var(--${string})`;
                'red-600': `var(--${string})`;
                'red-700': `var(--${string})`;
                'red-800': `var(--${string})`;
                'red-900': `var(--${string})`;
                'violet-050': `var(--${string})`;
                'violet-100': `var(--${string})`;
                'violet-200': `var(--${string})`;
                'violet-300': `var(--${string})`;
                'violet-400': `var(--${string})`;
                'violet-500': `var(--${string})`;
                'violet-600': `var(--${string})`;
                'violet-700': `var(--${string})`;
                'violet-800': `var(--${string})`;
                'violet-900': `var(--${string})`;
                'yellow-050': `var(--${string})`;
                'yellow-100': `var(--${string})`;
                'yellow-200': `var(--${string})`;
                'yellow-300': `var(--${string})`;
                'yellow-400': `var(--${string})`;
                'yellow-500': `var(--${string})`;
                'yellow-600': `var(--${string})`;
                'yellow-700': `var(--${string})`;
                'yellow-800': `var(--${string})`;
                'yellow-900': `var(--${string})`;
                black: `var(--${string})`;
                white: `var(--${string})`;
            };
            name: "backgroundColor";
            vars: {
                default: string;
            };
        };
        color: {
            dynamic: {
                default: string;
            };
            dynamicScale: {
                primary: `var(--${string})`;
                'primary-darker': `var(--${string})`;
                secondary: `var(--${string})`;
                'secondary-darker': `var(--${string})`;
                success: `var(--${string})`;
                'success-darker': `var(--${string})`;
                warning: `var(--${string})`;
                'warning-darker': `var(--${string})`;
                danger: `var(--${string})`;
                'danger-darker': `var(--${string})`;
                hint: `var(--${string})`;
                'hint-darker': `var(--${string})`;
                contrast: `var(--${string})`;
                'contrast-darker': `var(--${string})`;
                accent: `var(--${string})`;
                normal: `var(--${string})`;
                'normal-lighter': `var(--${string})`;
            };
            name: "color";
            vars: {
                default: string;
            };
        };
        opacity: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "opacity";
            vars: {
                default: string;
            };
        };
        pointerEvents: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "pointerEvents";
            vars: {
                default: string;
            };
        };
        overflow: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "overflow";
            vars: {
                default: string;
            };
        };
        textAlign: {
            dynamic: {
                default: string;
            };
            dynamicScale: true;
            name: "textAlign";
            vars: {
                default: string;
            };
        };
    };
} & {
    config: {
        paddingX: {
            mappings: ("paddingLeft" | "paddingRight")[];
        };
        paddingY: {
            mappings: ("paddingBottom" | "paddingTop")[];
        };
        marginX: {
            mappings: ("marginLeft" | "marginRight")[];
        };
        marginY: {
            mappings: ("marginBottom" | "marginTop")[];
        };
    };
}]>;
type Sprinkles = Omit<Parameters<typeof sprinkles>[0], 'color'> & {
    foregroundColor?: `${keyof typeof foreground}`;
};

interface BoxProps extends ComponentPropsWithoutRef<typeof Primitive.div>, Sprinkles {
}
declare const Box: react.ForwardRefExoticComponent<BoxProps & react.RefAttributes<HTMLDivElement>>;

export { Box, type BoxProps };
